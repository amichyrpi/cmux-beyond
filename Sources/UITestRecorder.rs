//! Rust port of [UITestRecorder.swift](UITestRecorder.swift).
//!
//! Phase 11 — test harness helper. Likely replaced entirely by Tauri's
//! WebDriver support (`tauri-driver`), so this module may remain empty.

// TODO(rewrite): port from UITestRecorder.swift (may be obsolete)
#[allow(dead_code)]
pub(crate) fn __link() {}
