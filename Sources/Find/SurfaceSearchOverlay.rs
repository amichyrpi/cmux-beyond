//! Rust port of [SurfaceSearchOverlay.swift](SurfaceSearchOverlay.swift).
//!
//! Phase 6 — terminal find overlay. Uses `alacritty_terminal::search` on
//! the Rust side and `@xterm/addon-search` on the frontend side.

// TODO(rewrite): port from SurfaceSearchOverlay.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
