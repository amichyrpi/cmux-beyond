//! Rust port of [AppDelegate.swift](AppDelegate.swift).
//!
//! Part of the Rust + Tauri rewrite. See `PLAN.md` at the repo root.
//!
//! Phase 5/9 — becomes thin glue once the Tauri frontend owns most app
//! lifecycle. macOS-specific menu items are cfg-gated and pulled in via
//! `objc2` in Phase 10.

// TODO(rewrite): port from AppDelegate.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
