//! Rust port of [cmuxApp.swift](cmuxApp.swift).
//!
//! Phase 5 — in the Swift app this is the `@main` SwiftUI `App`. In the
//! Rust port the equivalent is `cmux-rs/crates/cmux-app/src/main.rs`.
//! This file remains as a sibling marker so the mirror layout stays valid.

// TODO(rewrite): port from cmuxApp.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
