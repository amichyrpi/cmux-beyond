//! Rust port of [WindowDecorationsController.swift](WindowDecorationsController.swift).
//!
//! Phase 10 — window chrome (traffic lights, title bar). Tauri has a
//! `decorations` flag; macOS-specific behavior goes behind `cfg(macos)`
//! with `objc2`.

// TODO(rewrite): port from WindowDecorationsController.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
