//! Rust port of [TerminalWindowPortal.swift](TerminalWindowPortal.swift).
//!
//! Phase 5/6 — the Swift original hosts NSViews as portal layers above
//! SwiftUI. The Rust port is a Tauri `WebviewWindow` with the React app;
//! portals are replaced by React component composition.

// TODO(rewrite): port from TerminalWindowPortal.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
