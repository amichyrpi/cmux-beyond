//! Rust port of [SessionPersistence.swift](SessionPersistence.swift).
//!
//! Phase 4 — workspace / session save & restore. JSON on disk under the
//! platform data dir (via `dirs::data_dir()`).

// TODO(rewrite): port from SessionPersistence.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
