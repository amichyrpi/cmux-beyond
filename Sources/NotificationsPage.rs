//! Rust port of [NotificationsPage.swift](NotificationsPage.swift).
//!
//! Phase 8 — notifications page model. UI is React; this module owns the
//! store and exposes it via Tauri commands + events.

// TODO(rewrite): port from NotificationsPage.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
