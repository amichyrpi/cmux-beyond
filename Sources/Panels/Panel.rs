//! Rust port of [Panel.swift](Panel.swift).
//!
//! Phase 5 — base panel protocol. Becomes a Rust trait with methods like
//! `id`, `title`, `kind` (terminal | browser | markdown), and state
//! serialization for session persistence.

// TODO(rewrite): port from Panel.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
