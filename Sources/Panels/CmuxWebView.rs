//! Rust port of [CmuxWebView.swift](CmuxWebView.swift).
//!
//! Phase 7 — WKWebView wrapper becomes a Tauri `WebviewWindow` or nested
//! webview. Rust side only mediates JS ↔ Rust IPC via Tauri commands.

// TODO(rewrite): port from CmuxWebView.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
