//! Rust port of [BrowserPanel.swift](BrowserPanel.swift).
//!
//! Phase 7 — browser panel controller. Uses Tauri `WebviewWindow` as a child
//! webview hosted inside the Bonsplit layout.

// TODO(rewrite): port from BrowserPanel.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
