//! Rust port of [GhosttyConfig.swift](GhosttyConfig.swift).
//!
//! Phase 3 — parses `~/.config/ghostty/config`. The Rust app does not embed
//! Ghostty (it uses `alacritty_terminal` instead) but still honors the
//! ghostty config file for compatibility with user dotfiles.

// TODO(rewrite): port from GhosttyConfig.swift
#[allow(dead_code)]
pub(crate) fn __link() {}
