//! Tab model — ported from [Sources/TabManager.swift] and the Bonsplit
//! `TabItem` / `TabID` public types.
//!
//! Phase 4 of `PLAN.md`.

// TODO(rewrite): port tab identity, dirty flag, title, icon.
pub fn __link() {}
